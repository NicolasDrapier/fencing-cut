[project]
name = "fencing-cut"
version = "0.1.0"
description = "Automatic touch-by-touch cut detection for panning-camera fencing videos"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.4.6",
    "opencv-python-headless>=4.13.0.92",
    "scipy>=1.17.1",
]
